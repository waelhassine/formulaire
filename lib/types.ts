export interface Address {
  properties: {
    id: string;
    label: string;
    name: string;
    postcode: string; // Add postcode
    city: string; // Add city
    country: string; // Add country
  };
}
